# makes sure follow this step 

- 1. install the `req.txt` file 
    ```
    pip install -r req.txt 
    ```

- 2. clone the huggingface repo because in this repo have the pretrained vae weight.
    ```
    git clone https://huggingface.co/ProgramerSalar/vae_model_ckpt
    ```
    drop the folder `PATH/vae_ckpt` of `duffusion_pytorch_model.safetensors` file 